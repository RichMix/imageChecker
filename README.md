# imageChecker
A lightweight forensic triage tool for detecting suspicious extra data, hidden scanlines, appended payloads, and embedded file signatures in common image formats.  It was designed for CTF, steganography, and image-forensics workflows where an image may contain data that normal viewers ignore.
